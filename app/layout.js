import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "HireAI - Connect with Top AI Talent",
  description: "Find and hire the best AI professionals for your projects. Connect with skilled AI developers, data scientists, and machine learning experts.",
  keywords: "AI talent, hire AI developers, machine learning experts, data scientists, artificial intelligence jobs",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
